the dispatcher script names are either prefixed with "testing" or "target"

target prefix:
Examples of how the expected dispatchers when targeting a server should look like. 
Those scripts are just templates, you should make them work for your target.  

testing prefix:
The dispatchers used to test the *FTP.sh scripts and they shouldn't need any changes